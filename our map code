function manipulateString() { 
    var input = document.getElementById("inputString").value; 
    var output = manipulate(input); 
    document.getElementById("output").text
    Content = output;
} 
function manipulate(input) { // Perform string manipulation here 
    var result = "Manipulated: " + input; 
    return result; 
}
